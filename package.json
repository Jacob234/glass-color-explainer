{
  "name": "glass-color-explainer",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "node scripts/check_data.mjs && node scripts/check_catalog.mjs",
    "check:catalog": "node scripts/check_catalog.mjs",
    "test": "node --test 'test/**/*.test.mjs'"
  },
  "dependencies": {
    "@astrojs/svelte": "^7.2.5",
    "astro": "^5.0.0",
    "svelte": "^5.56.3"
  }
}
